import 'package:flutter/cupertino.dart';

class PhochinkiScreen extends StatelessWidget {
  const PhochinkiScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
